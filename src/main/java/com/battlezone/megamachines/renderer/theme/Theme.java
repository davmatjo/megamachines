package com.battlezone.megamachines.renderer.theme;

public enum Theme {
    DEFAULT;

    public String toString() {
        switch (this) {
            default:
                return "/default";
        }
    }
}